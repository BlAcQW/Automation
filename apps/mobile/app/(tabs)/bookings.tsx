import { Placeholder } from '@/components/Placeholder';

export default function BookingsScreen() {
  return <Placeholder title="Bookings" note="List, filters, detail, and payment links — next build-out." />;
}
