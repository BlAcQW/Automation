import { Placeholder } from '@/components/Placeholder';

export default function ChatsScreen() {
  return (
    <Placeholder
      title="Chats"
      note="WhatsApp inbox + human takeover — next build-out. Honors the 24h window & quota."
    />
  );
}
